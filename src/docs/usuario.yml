
/usuario:
  get:
    tags:
      - "Usuario"
    description: Listar todos os usuários
    responses:
      200:
        $ref: "#/components/responses/200"
      400:
        $ref: "#/components/responses/400"
      404:
        $ref: "#/components/responses/404"
      500:
        $ref: "#/components/responses/500"
      default:
        $ref: "#/components/responses/default"

/usuario/{id}:
  parameters:
    - name: "id"
      in: "path"
      description: "Id do usuário"
      required: true
      type: "integer"
      format: "int64"
  get:
    tags:
      - "Usuario"
    description: Listar todos os usuários
    parameters:
      - name: "ingressosComprados"
        in: "query"
        description: "Retornar usuário com seus respectivos ingressos comprados"
        schema:
            type: boolean
        default: true
    responses:
      200:
        $ref: "#/components/responses/200"
      400:
        $ref: "#/components/responses/400"
      404:
        $ref: "#/components/responses/404"
      500:
        $ref: "#/components/responses/500"
      default:
        $ref: "#/components/responses/default"

  put:
    tags:
      - "Usuario"
    parameters:
      - name: "body"
        in: "body"
        description: "Objeto usuario com os atributos que deseja alterar"
        required: true
    responses:
      200:
        $ref: "#/components/responses/200"
      400:
        $ref: "#/components/responses/400"
      404:
        $ref: "#/components/responses/404"
      500:
        $ref: "#/components/responses/500"
      default:
        $ref: "#/components/responses/default"

  delete:
    tags:
      - "Usuario"
    responses:
      200:
        $ref: "#/components/responses/200"
      400:
        $ref: "#/components/responses/400"
      404:
        $ref: "#/components/responses/404"
      500:
        $ref: "#/components/responses/500"
      default:
        $ref: "#/components/responses/default"
